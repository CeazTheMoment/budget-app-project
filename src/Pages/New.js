import TransactionNew from "../Components/TransactionNew";

function New() {
  return (
    <div className="Nuevo">
      <h2>Add a new transaction</h2>
      <TransactionNew />
    </div>
  );
}

export default New;
